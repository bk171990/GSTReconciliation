        <section class="main-content-wrapper">
            <!-- Main content view  -->
                             <div class="pageheader">
                <h1>View All Items</h1>
                <div class="breadcrumb-wrapper hidden-xs">
                    <span class="label">You are here:</span>
                    <ol class="breadcrumb">
                        <li><a href="/">Dashboard</a>
                        </li>
                        <li>Tables</li>
                        <li class="active">View Unit of Measure</li>
                    </ol>
                </div>
            </div>
            <section id="main-content" class="animated fadeInUp">
                      <div class="row">
                    <div class="col-md-12">
                        <div class="panel panel-default">
                          <div class="panel-heading">
                            <h3 class="panel-title">Unit of Measure</h3>
                            <div class="actions pull-right">
                             <i class="fa fa-expand"></i>
                                <i class="fa fa-chevron-down"></i>
                                <i class="fa fa-times"></i>
                            </div>
                          </div>
                          <div class="panel-body">
                            <%= link_to 'Add Unit of Measure', new_unit_of_measure_path, class: 'btn btn-link' %>
                        <table id="example" class="table table-striped table-bordered" cellspacing="0" width="100%">
                          <thead>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @unit_of_measures.each do |unit_of_measure| %>
      <tr>
        <td><%= unit_of_measure.name %></td>
        <td><%= link_to 'Show', unit_of_measure %></td>
        <td><%= link_to 'Edit', edit_unit_of_measure_path(unit_of_measure) %></td>
        <td><%= link_to 'Destroy', unit_of_measure, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
 </table>
                         
                       </div>
                     </div>
                </div>
            </div>
              </section>
<br>

